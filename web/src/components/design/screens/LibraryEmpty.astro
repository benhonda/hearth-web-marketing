---
/**
 * Flow I · II — Library, empty state.
 * Thin wrapper around Library.astro; see that file for the full screen.
 */
import Library from "./Library.astro";

interface Props { showTap?: boolean; }
const { showTap } = Astro.props;
---

<Library state="empty" showTap={showTap} />
